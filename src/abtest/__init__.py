"""abtest — an A/B experimentation engine done the senior way.

Frequentist (z/t-tests, power, sample size), Bayesian (Beta-Binomial posteriors, P(B>A),
expected loss), peeking-safe sequential testing (always-valid mSPRT), and CUPED variance
reduction — so an experiment is analyzed correctly, not just "is p < 0.05".
"""

__version__ = "0.1.0"
